{
  "name": "@hcyticketing/common",
  "version": "1.0.5",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "pub": "npm run build && git add . && git commit -m \"Prepare for patch release\" && npm version patch && git push && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "del-cli": "^6.0.0",
    "typescript": "^5.7.2"
  }
}
